configrationフォルダ内のseat_list.txtには、フェニックスホールの座席番号が入っている。
zaseki.txtには、予約済み座席の一覧が入っている。
zaseki_info.txtには、予約情報の通し番号が入っている。

reservation_infomationフォルダには、それぞれの予約情報が入っている。

keep.cはcomplete.c変更前の状態のファイル

reservation_information内のコンサート名のフォルダに、全角スペースは使えない。
もし全角スペースがある場合は、半角スペースに変更しなければならない。